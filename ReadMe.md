# Linuto - The Auto Linux Kernel Tool

- Linuto is a auto linux kernel tool on docker

## How to use?
### Add Kernel
```
./linuto.sh add_kernel <kernel source code .tar.xz>
```

### Kernel build
```
./linuto.sh build <Kernel Version>
```

### Kernel run
```
./linuto.sh run
```